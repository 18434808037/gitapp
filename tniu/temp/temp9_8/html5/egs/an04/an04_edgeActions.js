/***********************
* Adobe Edge Animate 组件动作
*
* 小心编辑这个文件，小心保存
* 函数签名和注释以“Edge”起始来维持
* 通过 Adobe Edge Animate 使动作可以交互
*
***********************/
(function($, Edge, compId){
var Composition = Edge.Composition, Symbol = Edge.Symbol; // 常用的 Edge 类别名

   //Edge symbol: 'stage'
   (function(symbolName) {
      
      
      Symbol.bindElementAction(compId, symbolName, "${Symbol_4}", "mouseover", function(sym, e) {
         // 当鼠标光标划在对象上（悬停）时插入的代码将运行
         // 从标签或特定时间开始播放时间轴。例如：
         // sym.play(500); 或 sym.play("myLabel");
         sym.getSymbol("Symbol_4").play(5001);
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Symbol_4}", "mouseout", function(sym, e) {
         // 当鼠标光标划出对象时（不再悬停）插入的代码将运行
         sym.getSymbol("Symbol_4").playReverse();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Symbol_8}", "mouseover", function(sym, e) {
         // 当鼠标光标划在对象上（悬停）时插入的代码将运行
         // 从标签或特定时间开始播放时间轴。例如：
         // sym.play(500); 或 sym.play("myLabel");
         sym.getSymbol("Symbol_8").play(4001);
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Symbol_8}", "mouseout", function(sym, e) {
         // 当鼠标光标划出对象时（不再悬停）插入的代码将运行
         sym.getSymbol("Symbol_8").playReverse();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Symbol_9}", "mouseover", function(sym, e) {
         // 当鼠标光标划在对象上（悬停）时插入的代码将运行
         // 从标签或特定时间开始播放时间轴。例如：
         // sym.play(500); 或 sym.play("myLabel");
         sym.getSymbol("Symbol_9").play(4001);
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Symbol_9}", "mouseout", function(sym, e) {
         // 当鼠标光标划出对象时（不再悬停）插入的代码将运行
         sym.getSymbol("Symbol_9").playReverse();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Symbol_3}", "mouseover", function(sym, e) {
         // 当鼠标光标划在对象上（悬停）时插入的代码将运行
         // 从标签或特定时间开始播放时间轴。例如：
         // sym.play(500); 或 sym.play("myLabel");
         sym.getSymbol("Symbol_3").play(5001);
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Symbol_3}", "mouseout", function(sym, e) {
         // 当鼠标光标划出对象时（不再悬停）插入的代码将运行
         sym.getSymbol("Symbol_3").playReverse();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Symbol_2}", "mouseover", function(sym, e) {
         // 当鼠标光标划在对象上（悬停）时插入的代码将运行
         // 从标签或特定时间开始播放时间轴。例如：
         // sym.play(500); 或 sym.play("myLabel");
         sym.getSymbol("Symbol_2").play(5001);
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Symbol_2}", "mouseout", function(sym, e) {
         // 当鼠标光标划出对象时（不再悬停）插入的代码将运行
         sym.getSymbol("Symbol_2").playReverse();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Symbol_10}", "mouseover", function(sym, e) {
         // 当鼠标光标划在对象上（悬停）时插入的代码将运行
         // 从标签或特定时间开始播放时间轴。例如：
         // sym.play(500); 或 sym.play("myLabel");
         sym.getSymbol("Symbol_10").play(4001);
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Symbol_10}", "mouseout", function(sym, e) {
         // 当鼠标光标划出对象时（不再悬停）插入的代码将运行
         sym.getSymbol("Symbol_10").playReverse();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Symbol_7}", "mouseover", function(sym, e) {
         // 当鼠标光标划在对象上（悬停）时插入的代码将运行
         // 从标签或特定时间开始播放时间轴。例如：
         // sym.play(500); 或 sym.play("myLabel");
         sym.getSymbol("Symbol_7").play(3001);
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Symbol_7}", "mouseout", function(sym, e) {
         // 当鼠标光标划出对象时（不再悬停）插入的代码将运行
         sym.getSymbol("Symbol_7").playReverse();
         
         sym.getSymbol("Symbol_7").playReverse();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Symbol_11}", "mouseover", function(sym, e) {
         // 当鼠标光标划在对象上（悬停）时插入的代码将运行
         // 从标签或特定时间开始播放时间轴。例如：
         // sym.play(500); 或 sym.play("myLabel");
         sym.getSymbol("Symbol_11").play(4001);
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Symbol_11}", "mouseout", function(sym, e) {
         // 当鼠标光标划出对象时（不再悬停）插入的代码将运行
         sym.getSymbol("Symbol_11").playReverse();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Symbol_6}", "mouseover", function(sym, e) {
         // 当鼠标光标划在对象上（悬停）时插入的代码将运行
         // 从标签或特定时间开始播放时间轴。例如：
         // sym.play(500); 或 sym.play("myLabel");
         sym.getSymbol("Symbol_6").play(5001);
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Symbol_6}", "mouseout", function(sym, e) {
         // 当鼠标光标划出对象时（不再悬停）插入的代码将运行
         sym.getSymbol("Symbol_6").playReverse();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Symbol_5}", "mouseover", function(sym, e) {
         // 当鼠标光标划在对象上（悬停）时插入的代码将运行
         // 从标签或特定时间开始播放时间轴。例如：
         // sym.play(500); 或 sym.play("myLabel");
         sym.getSymbol("Symbol_5").play(5001);
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Symbol_5}", "mouseout", function(sym, e) {
         // 当鼠标光标划出对象时（不再悬停）插入的代码将运行
         sym.getSymbol("Symbol_5").playReverse();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Symbol_13}", "mouseover", function(sym, e) {
         // 当鼠标光标划在对象上（悬停）时插入的代码将运行
         // 从标签或特定时间开始播放时间轴。例如：
         // sym.play(500); 或 sym.play("myLabel");
         sym.getSymbol("Symbol_13").play(4001);
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Symbol_13}", "mouseout", function(sym, e) {
         // 当鼠标光标划出对象时（不再悬停）插入的代码将运行
         sym.getSymbol("Symbol_13").playReverse();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Symbol_12}", "mouseover", function(sym, e) {
         // 当鼠标光标划在对象上（悬停）时插入的代码将运行
         // 从标签或特定时间开始播放时间轴。例如：
         // sym.play(500); 或 sym.play("myLabel");
         sym.getSymbol("Symbol_12").play(4001);
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Symbol_12}", "mouseout", function(sym, e) {
         // 当鼠标光标划出对象时（不再悬停）插入的代码将运行
         sym.getSymbol("Symbol_12").playReverse();
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Symbol_1}", "mouseover", function(sym, e) {
         // 当鼠标光标划在对象上（悬停）时插入的代码将运行
         // 从标签或特定时间开始播放时间轴。例如：
         // sym.play(500); 或 sym.play("myLabel");
         sym.getSymbol("Symbol_1").play(5001);
         

      });
      //Edge binding end

      Symbol.bindElementAction(compId, symbolName, "${Symbol_1}", "mouseout", function(sym, e) {
         // 当鼠标光标划出对象时（不再悬停）插入的代码将运行
         sym.getSymbol("Symbol_1").playReverse();
         

      });
      //Edge binding end

   })("stage");
   //Edge symbol end:'stage'

   //=========================================================
   
   //Edge symbol: 'Symbol_1'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 5000, function(sym, e) {
         // 在此处插入代码
         sym.stop();

      });
      //Edge binding end

   })("Symbol_1");
   //Edge symbol end:'Symbol_1'

   //=========================================================
   
   //Edge symbol: 'Symbol_2'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 5000, function(sym, e) {
         // 在此处插入代码
         sym.stop();

      });
      //Edge binding end

   })("Symbol_2");
   //Edge symbol end:'Symbol_2'

   //=========================================================
   
   //Edge symbol: 'Symbol_3'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 5000, function(sym, e) {
         // 在此处插入代码
         sym.stop();

      });
      //Edge binding end

   })("Symbol_3");
   //Edge symbol end:'Symbol_3'

   //=========================================================
   
   //Edge symbol: 'Symbol_4'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 5000, function(sym, e) {
         // 在此处插入代码
         sym.stop();

      });
      //Edge binding end

   })("Symbol_4");
   //Edge symbol end:'Symbol_4'

   //=========================================================
   
   //Edge symbol: 'Symbol_5'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 5000, function(sym, e) {
         // 在此处插入代码
         sym.stop();

      });
      //Edge binding end

   })("Symbol_5");
   //Edge symbol end:'Symbol_5'

   //=========================================================
   
   //Edge symbol: 'Symbol_6'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 5000, function(sym, e) {
         // 在此处插入代码
         sym.stop();

      });
      //Edge binding end

   })("Symbol_6");
   //Edge symbol end:'Symbol_6'

   //=========================================================
   
   //Edge symbol: 'Symbol_7'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 3000, function(sym, e) {
         // 在此处插入代码
         sym.stop();

      });
      //Edge binding end

   })("Symbol_7");
   //Edge symbol end:'Symbol_7'

   //=========================================================
   
   //Edge symbol: 'Symbol_8'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 4000, function(sym, e) {
         // 在此处插入代码
         sym.stop();

      });
      //Edge binding end

   })("Symbol_8");
   //Edge symbol end:'Symbol_8'

   //=========================================================
   
   //Edge symbol: 'Symbol_9'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 4000, function(sym, e) {
         // 在此处插入代码
         sym.stop();

      });
      //Edge binding end

   })("Symbol_9");
   //Edge symbol end:'Symbol_9'

   //=========================================================
   
   //Edge symbol: 'Symbol_10'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 4000, function(sym, e) {
         // 在此处插入代码
         sym.stop();

      });
      //Edge binding end

   })("Symbol_10");
   //Edge symbol end:'Symbol_10'

   //=========================================================
   
   //Edge symbol: 'Symbol_11'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 4000, function(sym, e) {
         // 在此处插入代码
         sym.stop();

      });
      //Edge binding end

   })("Symbol_11");
   //Edge symbol end:'Symbol_11'

   //=========================================================
   
   //Edge symbol: 'Symbol_12'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 4000, function(sym, e) {
         // 在此处插入代码
         sym.stop();

      });
      //Edge binding end

   })("Symbol_12");
   //Edge symbol end:'Symbol_12'

   //=========================================================
   
   //Edge symbol: 'Symbol_13'
   (function(symbolName) {   
   
      Symbol.bindTriggerAction(compId, symbolName, "Default Timeline", 4000, function(sym, e) {
         // 在此处插入代码
         sym.stop();

      });
      //Edge binding end

   })("Symbol_13");
   //Edge symbol end:'Symbol_13'

})(window.jQuery || AdobeEdge.$, AdobeEdge, "EDGE-84787");